{
  "private": true,
  "scripts": {
    "test": "nbb --classpath src:test -e \"(require '[clojure.test :as t] 'hl7-fhir.main-test 'hl7-fhir.validation-test) (let [r (t/run-tests 'hl7-fhir.main-test 'hl7-fhir.validation-test)] (.exit js/process (if (pos? (+ (:fail r) (:error r))) 1 0)))\""
  }
}
